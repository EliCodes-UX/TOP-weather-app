{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "using the new found method of using and fetching api's we will create a weather app for TOP",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "webpack --watch",
    "build": "webpack",
    "publish": "git subtree push --prefix dist origin gh-pages"
  },
  "keywords": [],
  "author": "EliCodes-UX",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.90.3",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "css-loader": "^6.8.1",
    "lodash": "^4.17.21"
  }
}
